<?xml version="1.0" encoding="utf-8"?>
<WatchFaceInfo>
    <Preview value="@drawable/preview" />
    <Category value="CATEGORY_EMPTY" />
    <AvailableInRetail value="true" />
    <!-- Editable must be true or the system editor never offers the
         complication slots, on the watch or in the phone app. -->
    <Editable value="true" />
    <!-- Lets people keep several configured versions as separate favorites. -->
    <MultipleInstancesAllowed value="true" />
</WatchFaceInfo>
